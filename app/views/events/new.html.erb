<!DOCTYPE html>
<html>
<head>
	<title>Creer evenement</title>
</head>
<body>
	<h1>Creation d'évenement</h1>
<div class="row justify-content-center">
    	<dir>
	    	<%= form_tag events_path(action: 'create'), method: "post" do %>

	      <%= label_tag "Titre" %>
	      <br />
	      <%= text_field_tag 'title', params[:title] %>
	      <br /> <br />

	      <%= label_tag "Date de l'evenement" %>
	      <br />
	      <%=datetime_field("event", "start_date", min: Date.today, default: Date.today)%>	      
	      <br /> <br />

	      <%= label_tag 'Durée' %>
	      <br />
	      <%= number_field_tag 'duration',0 %>
	      <br /> <br />      

	      <%= label_tag 'Description' %>
	      <br />
	      <%= text_area_tag 'description',"Une description",:rows=>5, :cols=>60 %>
	      <br /> <br />

	      <%= label_tag 'Prix' %>
	      <br />
	      <%= number_field_tag 'price',1 %>
	      <br /> <br />

	      <%= label_tag "Lieu" %>
	      <br />
	      <%= text_field_tag 'location', params[:location] %>
	      <br /> <br />

	      <%= submit_tag "Creer event" %>

	    	<% end %>
    	</dir>

    </div>
<% if @state == false  %>
	    <div class="row justify-content-center "style="padding-top: 1cm;">
			<div class="card card text-white bg-danger mb-3" style="width: 30cm;">
			<div class="card-header text-uppercase font-weight-bold">
			Attention erreur de saisis !
			</div>
			<div class="card-body">
			  <p>Nous n'avons pas réussi à creer l'event pour la (ou les) raison(s) suivante(s) :</p>
			  <ul>
			    <% @event.errors.full_messages.each do |message| %>
			      <li><%= message %></li>
			    <% end %>
			  </ul>
			</div>
			</div>
		</div>
	<% end %>

</body>
</html>
